{
  "name": "sweat-bridge-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore 보안 규칙 에뮬레이터 테스트 (T0)",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project sweat-bridge --config ../firebase.json \"node --test\"",
    "smoke:live": "node live-smoke.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "dotenv": "^16.4.5",
    "firebase": "^11.0.0"
  }
}
