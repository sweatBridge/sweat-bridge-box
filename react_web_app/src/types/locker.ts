export interface Lockers{
    number: number;
    state: string;
    id: string;  // 사용자 이메일
    realName: string;
    phone: string;
    assignee: string;
    note: string;
    startDate: string;
    endDate: string;
    createdAt: string;
}